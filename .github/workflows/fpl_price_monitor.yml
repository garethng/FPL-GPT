name: FPL Price Monitor

on:
  # 定时执行 - 每天运行3次（UTC时间）
  schedule:
    - cron: '30 0 * * *'   # 每天 07:30 UTC (北京时间 15:30)
    - cron: '0 12 * * *'   # 每天 12:00 UTC (北京时间 20:00)
  
  # 支持手动触发
  workflow_dispatch:

jobs:
  monitor-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout 代码
        uses: actions/checkout@v4
      
      - name: 🐍 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 安装依赖
        run: |
          cd fpl_price_monitor
          pip install -r requirements.txt
      
      - name: 🔍 运行价格监控
        run: |
          cd fpl_price_monitor
          python fetch_and_notify.py

